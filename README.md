# Billing_App


# System Dependencies (Linux)

# WeasyPrint requires native libraries. Install them on Ubuntu:

sudo apt update
sudo apt install -y libpango-1.0-0 libcairo2 libjpeg-dev libpng-dev libffi-dev libgdk-pixbuf2.0-0


